/**
 * this stores the acces and refresh token for convenience
 * in building the fetches of the components and pages
 */
export const ACCESS_TOKEN = "access";
export const REFRESH_TOKEN = 'refresh';
